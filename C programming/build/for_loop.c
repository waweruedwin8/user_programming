/**
 * for loop , syntax;
 * for (initialization expression; test expression; update expression)
 * {
 * // code inside the loop
 * }
*/
# include <stdio.h>
int main(){
    for(int i = 0; i < 10; i++) {
        printf("%d", i);
    }
    return 0;
}