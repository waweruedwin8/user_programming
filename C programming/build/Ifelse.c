#include <stdio.h>
int main ()

   int age;
    printf("enter your age\");
    scanf("%d", &age);
        if age >= 18 ;
            printf("You are elligible to vote\n");
        else 
            printf("not elligible to vote");

    
    return 0;
}
